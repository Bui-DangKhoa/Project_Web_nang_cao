{
  "name": "velour-shop-server",
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix velour-shop/server start",
    "install-server": "npm --prefix velour-shop/server install",
    "postinstall": "npm run install-server"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}